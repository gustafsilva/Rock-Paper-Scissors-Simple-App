import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 10
  },
  images: {
    marginTop: 10,
    alignItems: 'center',
  },
  image: {
    marginTop: 30
  }
});